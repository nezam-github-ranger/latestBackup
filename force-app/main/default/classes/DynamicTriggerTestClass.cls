@isTest(seeAllData = true) public class DynamicTriggerTestClass { private static testMethod void triggerTestInvocation () { try {  List < Project_Task__c > lstOfRecords = [SELECT Id FROM Project_Task__c]; if(!lstOfRecords.isEmpty()) { Database.update(lstOfRecords[0], false); } else { Project_Task__c record = new Project_Task__c(); Database.upsert(record, false); } } catch(Exception ex) { System.debug('Dynamic Trigger Creation Failed' + ex.getMessage()); } }}