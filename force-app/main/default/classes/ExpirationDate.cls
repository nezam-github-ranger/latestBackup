public class ExpirationDate implements Database.Batchable<sObject>, Database.Stateful, Schedulable {
	
    public ExpirationDate(){
        
    }
    
    public void execute(SchedulableContext SC) {
    	ExpirationDate expObj = new ExpirationDate();		    
    }
    
    public Database.QueryLocator start(Database.BatchableContext bc){
        System.debug('Execute start');
        return Database.getQueryLocator([SELECT Id, Name, SLAExpirationDate__c FROM Account WHERE SLAExpirationDate__c < Today]);
    }
    
    public void execute(Database.BatchableContext bc, List<Account> accountList){
        System.debug('Execute execute');
    	System.debug('accountList ==> ' + accountList);    
    }
    
    public void finish(Database.BatchableContext bc){
        System.debug('Execute finish');
        /*
        Messaging.EmailFileAttachment csvAttc = new Messaging.EmailFileAttachment();
        csvAttc.setFileName('DemoData.csv');
        csvAttc.setBody(Blob.valueOf(data));
        System.debug('csv data in blob format ==> ' + Blob.valueOf(data));
        Messaging.SingleEmailMessage email = new Messaging.SingleEmailMessage();
        email.setSubject('100000 Record CSV');
        email.setToAddresses( new List<String> {'nezam21nex@gmail.com'} );
        email.setPlainTextBody('CSV data generated by batch class!!');
        email.setFileAttachments(new Messaging.EmailFileAttachment[]{csvAttc});
        Messaging.SendEmailResult [] r = Messaging.sendEmail(new Messaging.SingleEmailMessage[] {email});
        System.debug('result ==> ' + r ); */
    }
}